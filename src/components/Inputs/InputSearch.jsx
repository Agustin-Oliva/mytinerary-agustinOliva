import Form from 'react-bootstrap/Form';
import Cities from '../../pages/CitiesPage/Cities';
function InputSearch() {
   /*  const handleInput = (e)=>{ 
      const citieSearch =  e.target.value;
      let query = '?'
      if(citieSearch){
        query += 'name' + citieSearch
      }
      getAllCities(query).then(setData)
    } */
  return (
    <>
     
     
     
    </>
  );
}

export default InputSearch;